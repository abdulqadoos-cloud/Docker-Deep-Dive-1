# Building and Pushing Docker Images to AWS ECR

## Project Overview
This project demonstrates how to containerize a lightweight application locally and securely push it to Amazon Elastic Container Registry (ECR) using a programmatic IAM user via the AWS CLI.

## Technologies Used
* Docker Engine
* AWS CLI v2
* Amazon ECR (Elastic Container Registry)
* IAM (Identity and Access Management)
* Linux CLI